{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Production'|trans }}{% endblock %}

{% block bread %}
    → <a href="{{ path('app_production') }}">{{ 'Production'|trans }}</a>
    → {{ 'Add Production'|trans }}
{% endblock %}

{% block body %}
    <div class="w-full">
        <!--Graph Card-->
        <div class="bg-white border rounded shadow">
            <div class="border-b p-3">
                <h5 class="font-bold uppercase text-gray-600">{{ 'Production Add'|trans }}</h5>
            </div>
            <div class="p-5">
                {{ include('production/_form.html.twig') }}
            </div>
        </div>
        <!--/Graph Card-->
    </div>
{% endblock %}