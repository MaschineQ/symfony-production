{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Packaging Material'|trans }}{% endblock %}

{% block bread %}
    → <a href="{{ path('app_category') }}">{{ 'Packaging Material'|trans }}</a>
    → {{ 'Edit Packaging Material'|trans }}
{% endblock %}

{% block body %}
    <div class="w-full">
        <!--Graph Card-->
        <div class="bg-white border rounded shadow">
            <div class="border-b p-3">
                <h5 class="font-bold uppercase text-gray-600">{{ 'Packaging Material Edit'|trans }}</h5>
            </div>
            <div class="p-5">
                {{ include('packaging_material/_form.html.twig') }}
            </div>
        </div>
        <!--/Graph Card-->
    </div>
{% endblock %}